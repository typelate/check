# ExecuteTemplate resolves when template receiver is passed as a function parameter.

check-templates
! stderr .

-- go.mod --
module example.com/app

go 1.25.0
-- main.go --
package main

import (
	"embed"
	"fmt"
	"html/template"
	"net/http"
)

var (
	//go:embed *.gohtml
	source embed.FS

	templates = template.Must(template.ParseFS(source, "*"))
)

type Page struct {
	Title string
}

func render(tpl *template.Template, w http.ResponseWriter, name string, data Page) {
	_ = tpl.ExecuteTemplate(w, name, data)
}

func handleIndex(w http.ResponseWriter, r *http.Request) {
	render(templates, w, "index.gohtml", Page{Title: "Home"})
}

var _ = fmt.Sprint

-- index.gohtml --
<h1>{{.Title}}</h1>
