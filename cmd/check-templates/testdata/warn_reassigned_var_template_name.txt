# ExecuteTemplate with a reassigned variable still warns.

check-templates -w
stderr 'ExecuteTemplate called with non-static template name \(W001\)'

-- go.mod --
module example.com/app

go 1.25.0
-- main.go --
package main

import (
	"embed"
	"fmt"
	"html/template"
	"net/http"
)

var (
	//go:embed *.gohtml
	source embed.FS

	templates = template.Must(template.ParseFS(source, "*"))
)

type Page struct {
	Title string
}

func handleIndex(w http.ResponseWriter, r *http.Request) {
	name := "index.gohtml"
	name = "other.gohtml"
	_ = templates.ExecuteTemplate(w, name, Page{Title: "Home"})
}

var _ = fmt.Sprint

-- index.gohtml --
<h1>{{.Title}}</h1>
-- other.gohtml --
<h2>{{.Title}}</h2>
