# ExecuteTemplate with a simple variable assignment resolves the template name.

check-templates
! stderr .

-- go.mod --
module example.com/app

go 1.25.0
-- main.go --
package main

import (
	"embed"
	"fmt"
	"html/template"
	"net/http"
)

var (
	//go:embed *.gohtml
	source embed.FS

	templates = template.Must(template.ParseFS(source, "*"))
)

type Page struct {
	Title string
}

func handleIndex(w http.ResponseWriter, r *http.Request) {
	name := "index.gohtml"
	_ = templates.ExecuteTemplate(w, name, Page{Title: "Home"})
}

var _ = fmt.Sprint

-- index.gohtml --
<h1>{{.Title}}</h1>
